{
  "name": "camp-doerfl-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs",
    "dev": "node src/dev.mjs",
    "preview": "node src/server.mjs",
    "cf:deploy": "npm run build && npx wrangler deploy",
    "cf:deploy:dry-run": "npm run build && npx wrangler deploy --dry-run"
  }
}
